# Shared MCP Configurations

This directory holds shared MCP (Model Context Protocol) server configurations referenced by `.agents/config.json` (`mcp-locations`).

Add shared MCP server definitions here as needed.
